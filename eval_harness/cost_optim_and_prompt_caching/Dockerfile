FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Cloud Run injects the PORT environment variable dynamically at runtime
CMD exec uvicorn test_caching:app --host 0.0.0.0 --port ${PORT:-8080}